import React from "react";
import s from './NewFeeds.module.css';
import { NavLink } from 'react-router-dom';

const NewFeeds = () => {
    return (
        <div className={s.newFeeds}>
            <h1>NEWSFEED</h1>
        </div>
    )
};

export default NewFeeds;
